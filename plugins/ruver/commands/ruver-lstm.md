---
description: Looks shit to me. Address review comments and merge conflicts on an existing PR. Verify, patch, reply, resolve threads. Never merge.
argument-hint: "<PR | review | comment URL>"
---

# /ruver-lstm

Follow **`~/.agents/skills/ruver-lstm/SKILL.md`** in full.

**Args:** `$ARGUMENTS`

Same PR. Same branch. Draft stays draft. Always rebase conflicts.
Load `receiving-code-review` before any patch.
Patch via `ruver-fd-coder` + TDD. Grill only if the fix is complicated.
ASK last resort (same rule as `/ruver-developer`).
Every GitHub reply runs `/pstack:unslop` first.
