# Node: spec

**Verb:** write. Matt Pocock `to-prd` as synthesis only. No interview here.

Write `$RUVER_ROOT/.ruver-feature-delivery/SPEC.md` from [../templates/SPEC.md](../templates/SPEC.md). English. Unslop. `technical-writing`.

Do not publish a PRD to the tracker. Do not wait for sign-off on the spec. Go to **tickets**. Chat a short rollup.

full_feature: optional `interrogate` on the spec (read-only). Act on findings: DECIDE from spec + repo. ASK only last-resort policy.

Main thread.
