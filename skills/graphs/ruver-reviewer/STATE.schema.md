# Reviewer STATE

**Path:** `.ruver-reviewer/STATE.md`

```
init | resolving | waiting_ci | reviewing | diagnosing | reporting | done
```

`review_verdict` · `failure_class` · `mergeable` · `ci` ·
`job_id` · `lane` · `worktree` · `worker_id` · `loop_id`

`loop_id` is copied from `.ruver-code-review/STATE.md` (one home).
