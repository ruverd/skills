# Node: resolve

**Verb:** inspect  
`gh pr view` + Linear if `DEV-\d+`.

Write STATE: `pr_url`, `repo`, `branch`, `sha`, `linear`.
Never invent ACs if Linear is down — continue on PR body.
Then **plan**.
