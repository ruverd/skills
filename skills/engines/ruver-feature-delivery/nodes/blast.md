# Node: blast

**Verb:** bundled `blast-radius`. Read-only.

Find the one fact the change is safe because of. Prove it by running code, not a writeup. Mark unproven facts as unproven.

Prove the safety fact or mark it unproven. Prefer the safer local change (DECIDE). Last-resort ASK only if the unproven fact is very important and you still cannot pick.

Skip on `light_change`. Then **quality** (thermo).
