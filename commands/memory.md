---
description: Alias /memory — same as /ruver-memory
argument-hint: "[--project] [<note>]"
---

# /memory

Alias of **`/ruver-memory`**. **Args:** `$ARGUMENTS`

Follow **`../skills/ruver-memory/SKILL.md`** in full —
this alias defines no steps of its own.
